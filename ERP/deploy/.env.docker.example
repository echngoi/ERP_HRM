# ============================================================
# ERP System — Docker Environment Variables
# Copy file này thành .env rồi sửa giá trị
# ============================================================

# --- Django ---
DEBUG=False
SECRET_KEY=THAY-BANG-KEY-NGAU-NHIEN-50-KY-TU

# ALLOWED_HOSTS: domain + IP máy chủ
ALLOWED_HOSTS=erp.congty.com,192.168.1.100,localhost,127.0.0.1

# CORS: frontend origins (có HTTPS nếu dùng Cloudflare)
CORS_ALLOWED_ORIGINS=https://erp.congty.com,http://192.168.1.100,http://localhost

# --- PostgreSQL ---
DB_ENGINE=django.db.backends.postgresql
DB_NAME=erp
DB_USER=erp
DB_PASSWORD=erp_secret_2026
DB_HOST=db
DB_PORT=5432

# --- ZKTeco / Ronald Jack ---
ZK_DEVICE_IP=192.168.1.225
ZK_DEVICE_PORT=5005
ZK_DEVICE_TIMEOUT=10
ZK_DEVICE_PASSWORD=0
ZK_PROTOCOL=adms
